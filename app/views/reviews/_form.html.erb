<%= simple_form_for [ restaurant, review ] do |f| %>
  <%= f.input :content, as: :text, input_html: { rows: 3 } %>
  <%= f.input :rating, collection: (0..5).to_a, prompt: "Select a rating" %>
  <%= f.submit "Post review", class: "btn btn-success mt-2" %>
<% end %>
